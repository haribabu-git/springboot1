node{
    stage('SCM Checkout'){
        git 'https://github.com/haribabu-git/springboot1'    
    }
    stage('Compile-package'){
        def mvnHome = tool name: 'MAVEN_HOME', type: 'maven';
        bat "${mvnHome}/bin/mvn -f springboot1/SpringBoot/pom.xml clean"         
    }
    
}
